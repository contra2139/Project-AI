# Phase 01: Setup & Dependencies
Status: ✅ Complete
Dependencies: None

## Objective
Cài đặt các thư viện cần thiết và chuẩn bị cấu trúc thư mục cho các thành phần UI mới.

## Implementation Steps
1. [x] Cài đặt `lightweight-charts`.
2. [x] Kiểm tra lại `package.json` xem `recharts` đã được cài đặt đúng chưa.
3. [x] Tạo cấu trúc thư mục `src/components/signals`.

## Test Criteria
- [ ] `npm list lightweight-charts` trả về version 4.x.
- [ ] Build project không lỗi.
